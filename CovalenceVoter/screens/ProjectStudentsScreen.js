import React, { Component } from 'react';
import { ScrollView, Text } from 'react-native';

export default class ProjectStudentsScreen extends Component {

    render() {
        return (
            <ScrollView>
                <Text>Students and stuff</Text>
            </ScrollView>);
    }
}